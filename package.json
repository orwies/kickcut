{
  "name": "kickcut",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "gen-cert": "node certs/gen-cert.js",
    "setup": "npm install && node certs/gen-cert.js",
    "dev:storage": "cd storage-server && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "devDependencies": {
    "selfsigned": "^2.4.1"
  }
}
